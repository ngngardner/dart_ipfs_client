name: dart_ipfs_client
description: A client library for the IPFS HTTP API.
version: 0.0.1
homepage: https://github.com/ngngardner/dart_ipfs_client

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  chopper: ^4.0.5
  json_annotation: ^4.4.0
  json_serializable: ^6.1.6

dev_dependencies:
  build_runner: ^2.1.10
  chopper_generator: ^4.0.5
  logging: ^1.0.2
  pedantic: ^1.9.0
  test: ^1.14.4
